    import XCTest
    @testable import LystAssets

    final class LystAssetsTests: XCTestCase {
        func testExample() {
            // This is an example of a functional test case.
            // Use XCTAssert and related functions to verify your tests produce the correct
            // results.
            XCTAssertEqual(LystAssets().text, "Hello, World!")
        }
    }
